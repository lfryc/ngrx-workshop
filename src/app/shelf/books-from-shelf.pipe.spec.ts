import { BooksFromShelfPipe } from './books-from-shelf.pipe';

describe('BooksFromShelfPipe', () => {
  it('create an instance', () => {
    const pipe = new BooksFromShelfPipe();
    expect(pipe).toBeTruthy();
  });
});
